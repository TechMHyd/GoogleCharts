describe("productController Specs for Sample Testing", function(){	 var appCtrl,scope;	 //mock Application to allow us to inject our own dependencies	 beforeEach(module("productModule"));	 //mock the controller for the same reason and include $rootScope and $controller	 beforeEach(inject(function ($controller,$rootScope){	 	 //create an empty scope     scope = $rootScope.$new();	 //declare the controller and inject our empty scope	 appCtrl = $controller("productController",{$scope: scope});		 }));	Xit("Testing execution of dopass function",inject(function(){ 		  var postToAccept = {//sample json data							code: 'Title',							fname: 'Post'						  };          initialize = spyOn(scope,'dopass').and.callThrough();;	      var response=scope.dopass(postToAccept);	      expect(scope.dopass).toHaveBeenCalled();	  })); });